Write clients in one function in client.rs which returns, public ip and port and take in username 
well have two clients 
one under the register button -> which then makes u an active peer acc to the server
and one find -> which gives public ip and port of other peer, only needed if this user is sending